For Windows:

```shell
make install-base            # To install no-dev requirements
make run-calculator          # To run the calculator
make run-factorial           # To run the factorial
make run-infinite-array      # To run the infinite array
make run-sortirovka-massiva  # To run the sortirovka massiva
```